# WS2 Remap Table — old (`origin/main`) → new five-surface IA

**Date:** 2026-06-09
**Source:** every content page on `origin/main` (85 files; excludes `docs/plans/` and `docs/_partials/`), captured **before PR #43 merges** while `origin/main` still exposes the old tree. Pre-merge SHA: **`653b3f3`** — after merge, source old content via `git show 653b3f3:docs/...`.
**Purpose:** input to WS2 (content migration) and WS8 (redirect map). Old URLs use `trailingSlash: false`, so an `index.mdx` serves at `/docs/<dir>`.

**Legend — Action:**
- `migrate` = hand-move prose into the named placeholder.
- `regenerate` = auto-generated by WS3 (OpenAPI / GraphQL); do **not** hand-migrate — redirect the URL prefix once new slugs exist.
- `external` = link out (not hosted here); 301 to the adgem.com page.
- `merge` = fold into an existing target page.
- `DECISION` = no clean home in the locked IA — needs your/Gus' call (see §Gaps).

---

## 1. Get Started

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `getting-started/index.mdx` | `/docs/getting-started` | `get-started/index` | migrate | |
| `getting-started/essentials-overview.mdx` ("Why AdGem?") | `/docs/getting-started/essentials-overview` | `get-started/welcome` | migrate | Value-prop content → Welcome. |
| `getting-started/app-property-setup.mdx` | `/docs/getting-started/app-property-setup` | `get-started/quickstart` | migrate | Dashboard app/property setup = first quickstart step. |
| `getting-started/preflight-checks.mdx` | `/docs/getting-started/preflight-checks` | `get-started/quickstart` *(or `decide`)* | migrate | DECISION-lite: readiness checklist — quickstart vs decide. |

New pages with **no old source** (fresh content in WS2): `get-started/core-concepts` (partly from `offers/*`, see §Gaps), `get-started/decide`.

## 2. Integrate

### 2a. Offer Delivery — Pre-built (SDKs + Web Offerwall)

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `integrations/web-offerwall/index.mdx` | `/docs/integrations/web-offerwall` | `integrate/offer-delivery/pre-built/web-offerwall` | migrate | |
| `integrations/android-sdk/index.mdx` | `/docs/integrations/android-sdk` | `integrate/offer-delivery/pre-built/android-sdk` | migrate | |
| `integrations/ios-sdk/index.mdx` | `/docs/integrations/ios-sdk` | `integrate/offer-delivery/pre-built/ios-sdk` | migrate | |
| `integrations/unity-sdk/index.mdx` | `/docs/integrations/unity-sdk` | `integrate/offer-delivery/pre-built/unity-sdk` | migrate | |
| `integrations/{android,ios,unity}-sdk/changelog.mdx` (×3) | `/docs/integrations/*-sdk/changelog` | **DECISION** | DECISION | Skeleton has single page per SDK, no `changelog` subpage. WS9 wants per-SDK changelogs preserved. Add `changelog` subpages, or aggregate into `resources/changelog`. |
| `integrations/web-offerwall/changelog.mdx` | `/docs/integrations/web-offerwall/changelog` | **DECISION** | DECISION | Same as above for the Web Offerwall. |
| `integrations/{android,ios,unity}-sdk/optional-parameters.mdx` (×3) | `/docs/integrations/*-sdk/optional-parameters` | each SDK page | merge *(or subpage)* | Fold into the SDK page, or add a subpage per SDK. |
| `integrations/{android,ios,unity}-sdk/example-app.mdx` (×3) | `/docs/integrations/*-sdk/example-app` | each SDK page | merge *(or subpage)* | Same. |
| `integrations/web-offerwall/customization.mdx` | `/docs/integrations/web-offerwall/customization` | **split** → `configure/customization` + `integrate/.../web-offerwall` | migrate (split, dedupe) | Real 85-line page that **overlaps** with `configuration/offer-wall-customization` (§3). Currency name/icon/multiplier → `configure/customization` (dedupe against the §3 page); iFrame/WebView + native-UI best practices → the Web Offerwall integration page. |

### 2b. Offer Delivery — Partner-built

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| *(Prism narrative)* | — | `integrate/offer-delivery/partner-built/prism` | fresh | Integration narrative for Prism; spec detail lives in Reference (§4). Draw from `targeted-api/offers-endpoint` + overview. |
| *(Offer API narrative)* | — | `integrate/offer-delivery/partner-built/offer-api` | fresh | Same pattern for the REST Offer API. |

### 2c. Reward Mechanism (postbacks live here, not Configure)

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `webhooks/index.mdx` ("Webhooks & Postbacks") | `/docs/webhooks` | **split** → `integrate/reward-mechanism/index` + `configure/webhooks` | migrate (split) | Postback intro → Integrate; webhook config → Configure. |
| `webhooks/postback-options.mdx` ("Postback Options" — GET vs POST) | `/docs/webhooks/postback-options` | `integrate/reward-mechanism/postbacks-v3` + `postbacks-v2` | migrate (split) | GET=v2, POST=v3 content. Apply postback cleanup (no beta, no `[Deprecated]` on v2, strip `v2/v3` from example endpoints). |
| `webhooks/offer-api-postback-setup.mdx` ("Postback Setup") | `/docs/webhooks/offer-api-postback-setup` | `integrate/reward-mechanism/postbacks-v3` (+ `postbacks-v2`) | migrate | Setup steps for both postback types. |

New pages with **no old source**: `integrate/overview`, `offer-delivery/index`, `pre-built/index`, `partner-built/index`, `reward-mechanism/index` (partly from `webhooks/index`), `reward-mechanism/client-polling` (NEW — document the SDK polling mechanism), `reward-mechanism/third-party`.

## 3. Configure

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `configuration/index.mdx` | `/docs/configuration` | `configure/index` | migrate | |
| `configuration/offer-wall-customization.mdx` | `/docs/configuration/offer-wall-customization` | `configure/customization` | migrate | Canonical customization content. |
| `configuration/offer-wall-promotions.mdx` | `/docs/configuration/offer-wall-promotions` | `configure/promotions` | migrate | |
| `webhooks/offer-event-webhooks.mdx` | `/docs/webhooks/offer-event-webhooks` | `configure/webhooks` *(or `reference/webhook-events`)* | migrate | DECISION-lite: config narrative → Configure; event schema → Reference. |
| `webhooks/suspended-player-webhooks.mdx` | `/docs/webhooks/suspended-player-webhooks` | `configure/webhooks` *(or `reference/webhook-events`)* | migrate | Same split question. |

New pages with **no old source**: `configure/security`, `configure/sandbox`.

## 4. Reference

### 4a. Auto-generated — regenerated by WS3 (do not hand-migrate)

| Old path/prefix | Old URL prefix | New target | Action | Notes |
|---|---|---|---|---|
| `integrations/offer-api/*` (OpenAPI: 11 files — info/tag/`*.api`/`schemas/*`) | `/docs/integrations/offer-api/*` | `reference/offer-api/*` | regenerate | WS3 repoints OpenAPI `outputDir`. Redirect prefix `/docs/integrations/offer-api/` → `/docs/reference/offer-api/`; finalize per-page slugs after regen. |
| `integrations/targeted-api/{generated.md, operations/*, types/*}` (GraphQL: ~19 files) | `/docs/integrations/targeted-api/*` | `reference/prism/schema/*` | regenerate | WS3 repoints GraphQL `baseURL` to `reference/prism` and rebrands Targeted API → Prism. |
| `api-reference/index.mdx` ("Reporting API") | `/docs/api-reference` | `reference/reporting-api/overview` | migrate | Hand-written; not part of the generators. |

### 4b. Targeted API hand-maintained → Prism (rebrand)

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `integrations/targeted-api/authentication.mdx` | `/docs/integrations/targeted-api/authentication` | `reference/prism/authentication` | migrate | Rebrand to Prism. |
| `integrations/targeted-api/tokens.mdx` | `/docs/integrations/targeted-api/tokens` | `reference/prism/tokens` | migrate | |
| `integrations/targeted-api/offers-endpoint.mdx` | `/docs/integrations/targeted-api/offers-endpoint` | `reference/prism/overview` | migrate | GraphQL entry-point doc → overview (also feeds partner-built/prism narrative). |
| `integrations/targeted-api/playground.mdx` ("Targeted API Playground") | `/docs/integrations/targeted-api/playground` | `reference/prism/playground` | migrate | Rebrand; also a WS5 sandbox item. |
| `integrations/targeted-api/faq.mdx` | `/docs/integrations/targeted-api/faq` | **DECISION** | DECISION | No FAQ slot in the Prism Reference sidebar (overview/auth/tokens/errors/versioning/schema/playground/recipes). Fold into `recipes`, or add a Prism FAQ, or move to Resources. |

New pages with **no old source**: `reference/index`, `reference/prism/{errors,versioning,recipes}`, `reference/offer-api/{authentication,pagination,errors,versioning}` overviews, `reference/reporting-api/{authentication,errors}`, `reference/webhook-events`, `reference/errors`.

## 5. Resources

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `resources/index.mdx` | `/docs/resources` | `resources/index` | migrate | |
| `resources/terms-glossary.mdx` ("Terms Glossary") | `/docs/resources/terms-glossary` | `resources/glossary` | migrate | |
| `resources/branding-assets.mdx` | `/docs/resources/branding-assets` | `resources/branding-assets` | migrate | |
| `resources/player-support-overview.mdx` | `/docs/resources/player-support-overview` | `resources/` (keep as bridge/link-out) | migrate | Per PUB-204: player support is a SEPARATE instance; keep only this overview that links out. Not in skeleton sidebar yet — add it. |
| `resources/developer-support.mdx` ("Developer Support") | `/docs/resources/developer-support` | **DECISION** | DECISION | No slot in new Resources. Keep as a Resources page, fold into footer/support chrome, or drop. |
| `resources/product-roadmap.mdx` ("Product Roadmap") | `/docs/resources/product-roadmap` | **DECISION** | DECISION | No slot. Keep, externalize, or drop. |
| `resources/service-status.mdx` ("Service Status") | `/docs/resources/service-status` | `external` → `status.adgem.com` | external | Footer already links Service Status; redirect this page out or drop. |
| `resources/support-urls.mdx` ("Support URLs") | `/docs/resources/support-urls` | **DECISION** | DECISION | Looks like an internal reference list — keep in Resources or drop. |

New pages with **no old source**: `resources/changelog` (aggregate; may absorb per-product changelogs — see §2a DECISION), `resources/migrations/{overview, static-api-to-offer-api, postback-v2-to-v3}`.

## 6. Legal — external link-out (NOT hosted here)

| Old path | Old URL | New target | Action | Notes |
|---|---|---|---|---|
| `legal/privacy-policy.mdx` | `/docs/legal/privacy-policy` | `https://adgem.com/privacy-policy/` | external | |
| `legal/terms-and-conditions.mdx` | `/docs/legal/terms-and-conditions` | `https://adgem.com/terms-and-conditions/` | external | |
| `legal/sdk-license-agreement.mdx` | `/docs/legal/sdk-license-agreement` | **DECISION** | DECISION | No external URL found on adgem.com footer. Confirm where the SDK License Agreement lives (Iliana) before redirecting. |
| `legal/index.mdx` | `/docs/legal` | `https://adgem.com/` *(or a `/legal` index if marketing creates one)* | external | adgem.com has no `/legal` index today (404). |

---

## Gaps / decisions needed (no clean home in the locked IA)

1. **Payments / monetization (3 pages) — no home.** `monetization/{index, payment-guide, payment-setup}` ("how publishers get paid", payout terms, dashboard payment setup). The five surfaces have no payments page. Options: add `configure/payments` (setup is config-like) + put payout *terms* in Resources; or a single Resources page; or a new section. **Needs your/Gus' call** — this is real publisher content with nowhere to land. Old URLs: `/docs/monetization`, `/docs/monetization/payment-guide`, `/docs/monetization/payment-setup`.
2. **Offer concepts (3 pages).** `offers/{index, multi-reward-offers, completion-difficulty-scale}` are educational. Recommend: `offers/index` + `multi-reward-offers` → `get-started/core-concepts`; `completion-difficulty-scale` (a 1–7 reference table) → `resources/glossary` or a Reference page. Confirm.
3. **Per-SDK supporting pages (~13).** changelog/example-app/optional-parameters × {android, ios, unity} + web-offerwall changelog. Skeleton has one page per SDK. Decide: subpages per SDK vs. merge-in vs. aggregate changelogs into `resources/changelog`. (WS9 wants per-SDK changelogs to survive.)
4. **Prism FAQ** (`targeted-api/faq`) — no slot; fold into `recipes`, add a FAQ, or Resources.
5. **Resources extras** (`developer-support`, `product-roadmap`, `support-urls`) — no slots; keep/externalize/drop each.
6. **Webhook events split** — decide whether `offer-event-webhooks` / `suspended-player-webhooks` narrative goes to `configure/webhooks` (config) and/or `reference/webhook-events` (schema).

## Count reconciliation
- 85 total on `origin/main`.
- ~30 auto-generated (regenerate via WS3: 11 OpenAPI + ~19 GraphQL).
- 4 legal → external.
- ~38 authored prose → migrate/merge per tables above.
- Remainder are the DECISION rows in §Gaps.
